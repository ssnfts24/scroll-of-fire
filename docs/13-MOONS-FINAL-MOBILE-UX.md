# 13 Moons Final Mobile UX Stabilization

## Files Changed
- `/home/runner/work/scroll-of-fire/scroll-of-fire/docs/moons.html`
- `/home/runner/work/scroll-of-fire/scroll-of-fire/docs/assets/css/moons.css`
- `/home/runner/work/scroll-of-fire/scroll-of-fire/docs/assets/js/moons.js`
- `/home/runner/work/scroll-of-fire/scroll-of-fire/docs/assets/js/pwa.js`
- `/home/runner/work/scroll-of-fire/scroll-of-fire/docs/assets/js/moons-images.js`

## Final Mobile DOM Order (Today experience)
1. Main site header
2. Living signal strip
3. Compact Remnant 13 Moons introduction
4. Living Calendar notice
5. Compact app shortcuts (`Download App`, `iPhone / iPad Instructions`)
6. Main tab navigation (`.tabs`, single row)
7. Compact Today summary card + Today actions card
8. Active Moon artwork
9. Moon progress ring + selected-day controls
10. Command Center statistics
11. Remaining Today content
12. Other tab panels
13. App Settings / install / recovery / diagnostics

## Final Tab Behavior
- Main tabs remain the single authoritative navigation row.
- Tabs are in normal document flow (not fixed-bottom and not sticky docked nav).
- Horizontal scrolling remains enabled on narrow viewports.
- Active tab state continues using `aria-selected`, `tabindex`, and `.active`.
- Duplicate mobile dock markup was removed from `moons.html`.

## Movement Data Source
- Today movement uses the same deterministic moon-day mapping (`DAY_ARCHETYPES` in `moons.js`) used by the moon engine.
- Fallback source remains shared state (`window.CodexState.dailyMovement`) when available.
- Final fallback text when shared state is unavailable:
  - `Open today's full reading to view the current movement.`
- Movement updates through existing render pathways used by date controls, selected date, timezone changes, boundary mode, sunset refresh, and deep-link date selection.

## App Shortcut Destinations
- `Download App`:
  - Activates `settingsPanel`
  - Scrolls to `#appInstallCard`
  - Focuses `#installApp`
- `iPhone / iPad Instructions`:
  - Activates `settingsPanel`
  - Expands `#iosInstallHelp`
  - Scrolls to `#iosInstallHelp`
  - Focuses `#iosInstallInstructions`

## Active Moon Artwork Sizing/Centring
- Artwork remains source-generated by `moons-images.js` (no asset edits).
- Wrapper: centered flex container, normal flow, bounded by parent (`.current-moon-art`).
- Image: `width: 100%`, capped max width, `height: auto`, `object-fit: contain`, centered margins.
- `moon-badge` with artwork now stacks in a vertical order (`art -> ring -> details`) to avoid right-shift and clipping.

## Patterns Relocation
- Patterns statistic remains in Command Center (`#statPatterns`).
- It is no longer stranded above artwork because Command Center was moved lower.
- On narrow mobile (`max-width: 760px`), `#statPatternsCard` is hidden when count is `0`.

## Share-Card Verification
- Share card engine was preserved (no rebuild).
- Existing centered movement wrapping logic (`drawCenteredWrappedText`) remains in `share-day.js`.
- Existing square/story limits and footer protections remain unchanged.

## Media Query / Responsive Notes
- Mobile dock compensation spacing was removed.
- Tabs kept compact horizontal scroll behavior.
- App shortcuts remain two-column and stack under `350px`.
- Reduced motion now forces non-smooth scroll behavior in JS tab/install jumps.

## Accessibility Behavior
- Tab keyboard semantics remain in `setupTabs()`.
- Hidden panels remain hidden via `panel.hidden` in tab activation.
- Install shortcut destinations now focus real targets (`#installApp`, `#iosInstallInstructions`).
- Reduced-motion preference is respected by tab/install scroll behavior.

## Tests Performed
- JS syntax: `for f in docs/assets/js/*.js; do node --check "$f"; done`
- Data validation: `node scripts/validate-codex-data.js`
- Tests: `node --test tests/site-links.test.js tests/pwa-resilience.test.js tests/share-day.test.js`

## Known Remaining Limitations
- Existing `tests/pwa-resilience.test.js` has two failing tests in this environment related to install UI expectations.
- No browser-device automation is included in this repo; responsive/device verification remains manual.
