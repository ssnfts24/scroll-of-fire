The complete final Codex site payload is stored in docs/_codex_final_upload as 6 independent ZIP archives named CODEX-FINAL-UPLOAD-PART-*.zip.

Perform these actions on the current branch only:

1. Create a clean temporary extraction directory outside docs.
2. Extract every archive into the same temporary directory. They all contain a shared payload/ tree.
3. Verify UPLOAD-MANIFEST.json checksums if available.
4. Replace the repository docs/ tree with the extracted payload/docs/ tree. Use rsync --delete or equivalent so obsolete files are removed, but do not touch .git.
5. Copy payload/netlify.toml to the repository root.
6. Replace repository tests/ with payload/tests/.
7. Delete docs/_codex_final_upload after successful extraction.
8. Run: node --test tests/*.test.js
9. Run node --check on every JavaScript file under docs/.
10. Check git status and report exact added, modified, and deleted files.
11. Do not merge into main. Commit to this branch and open a pull request.
12. Wait for the Netlify Deploy Preview. Do not merge until the preview is manually reviewed.

Expected automated test result: 438 passing, 0 failing.
