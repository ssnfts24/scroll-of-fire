# Current Build Defect Inventory

Audit date: 2026-08-15
Branch scope: advanced site and Living Time Observatory rebuild
Pages exercised: all 69 HTML pages by exhaustive audit; production baseline inspected at the homepage and full Sphere workspace

## Defects

| ID | Severity | Visible symptom | Affected page | Source file | Probable cause | Exact reproduction | Expected behavior | Test coverage | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEF-001 | BLOCKER | Sphere page fails during startup; 3D/SVG workflows never fully initialize | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js` | `_selectedDayFromMarker` was accidentally scoped inside `_readLocalJson`, causing `ReferenceError` in `applyUrlState()` | Open `living-time-sphere.html` and check console before fix: `ReferenceError: _selectedDayFromMarker is not defined` | URL state and saved selected date should parse without runtime errors | `tests/living-time-sphere-3d.test.js` | **FIXED** |
| DEF-002 | CRITICAL | Selected date authority could not restore correctly from URL marker and saved state | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js` | `_restoreSelectedStateIfNeeded` and related helpers were also incorrectly nested and unreachable | Open page with marker query (for example `?marker=day-120`) and observe startup failure before fix | Selected day should survive initialization and remain authoritative | `tests/living-time-sphere-3d.test.js` | **FIXED** |
| DEF-003 | CRITICAL | Location/place persistence fails across reloads; weather repeatedly appears unconfigured | `living-time-sphere.html` | `docs/assets/js/environment/open-meteo-adapter.js` | `safeWrite` rejected all keys except units, silently dropping active place, places list, and cached snapshots | Set manual coordinates, reload page, inspect location/weather state before fix | Active place and weather snapshot should persist safely in local storage | `tests/environmental-sphere-workflow.test.js` | **FIXED** |
| DEF-004 | HIGH | Date classification labels did not match required live/forecast/historical taxonomy | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js` | `_classifyActiveDate` used coarse labels (`Current civil time`, generic historical) | Select dates across current/future/past contexts and inspect range badge before fix | Classification should map to `LIVE CURRENT`, `FORECAST`, `HISTORICAL FORECAST`, `REANALYSIS`, `UNAVAILABLE` | `tests/environmental-sphere-workflow.test.js` (indirect) | **FIXED** |
| DEF-005 | MEDIUM | External resources blocked in sandbox produce console noise | `index.html`, `moons.html`, `living-time-sphere.html` | third-party URLs in HTML headers | Blocked network for Google Fonts / GTM in test environment | Open pages in restricted environment and inspect console | Third-party failures should not break core rendering | `tests/site-links.test.js` (partial) | ACCEPTED (non-blocking external dependency) |
| DEF-006 | MEDIUM | WebGL software fallback warnings in headless browser | `living-time-sphere.html` | Browser runtime / GPU stack | Headless WebGL fallback behavior, not app logic failure | Open sphere page in headless browser; observe WebGL deprecation warnings | App should continue with working renderer path | `tests/living-time-sphere-3d.test.js` | ACCEPTED (environment warning) |
| DEF-007 | MEDIUM | Weather classification for reanalysis range still depends on provider capabilities not yet implemented | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js`, provider stack | Reanalysis provider pathway is not currently wired into environment provider layer | Select older historical dates and inspect weather availability messaging | Reanalysis should be available when provider support is implemented | No dedicated reanalysis integration test | PARTIAL |
| DEF-008 | LOW | Optional environment sublayers `airQuality` and `spaceWeather` remain placeholders | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-renderer-3d.js` | Layer hooks exist but no data mapping yet | Enable environment layer with available data and inspect diagnostics | Optional layer failure/absence should not break core 3D renderer | `tests/living-time-sphere-3d.test.js` (interface checks) | PARTIAL (safe optional disable) |
| DEF-009 | BLOCKER | Homepage Sphere frame is blank and its telemetry never initializes on the deployed build | `index.html` | `docs/assets/js/home-observatory-instrument.js` | Incomplete dependency stack and destructive intersection lifecycle prevented a durable shared mount | Open the production homepage, scroll to the Sphere, and observe the empty field on the baseline deployment | Paint a baseline renderer immediately, upgrade to 3D when capable, and preserve the mount while offscreen | `tests/advanced-observatory-rebuild.test.js` | **FIXED** |
| DEF-010 | CRITICAL | Stable JS/CSS filenames can remain stale after a deploy | site-wide | `docs/service-worker.js`, `netlify.toml` | Long-lived CDN headers plus stale-while-revalidate ignored version query strings | Deploy a changed stable asset and revisit from a previously controlled browser | Source assets must network-check on every visit/deploy while retaining an offline copy | PWA and advanced rebuild cache-policy tests | **FIXED** |
| DEF-011 | HIGH | Full Sphere page is visually dominated by controls; 3D outcome is unclear | `living-time-sphere.html` | shared mount, Sphere CSS, workspace CSS | Renderer state was not observable and large-screen disclosure CSS forced advanced controls open | Open the Sphere on desktop and compare sphere footprint to the command deck | Sphere is the primary visual field; controls are collapsible; active renderer/fallback reason is visible | advanced rebuild and existing Sphere suites | **FIXED** |
| DEF-012 | HIGH | Records, questions, quests, recurrence, and century map shells have no behavior | `living-time-sphere.html` | missing Observatory workspace controller | Markup existed without a bound local-first application layer | Use any Observatory control in the baseline deployment | Every local workspace control validates, persists, renders, imports/exports, or explains failure | advanced rebuild workspace tests | **FIXED** |
| DEF-013 | HIGH | Several live decorative images are broken despite files existing in source | homepage and long-form pages | deploy/cache artifact mismatch | The deployed artifact did not match the audited source and older caches could preserve missing responses | Inspect the production homepage gates/registry images | Existing images deploy with bounded caching and broken-resource shells collapse without leaving empty cards | site reference audit and shared broken-media handling | **FIXED IN SOURCE / VERIFY AFTER DEPLOY** |
| DEF-014 | MEDIUM | Missing descriptions, headings, canonicals, anchors, and dead internal assets reduce discoverability and accessibility | multiple routes | page HTML and shared Codex CSS | Legacy and redirect pages were outside prior route-level validation | Run a complete route/reference crawl | Every deployed page has one H1, useful metadata, valid canonical, and resolvable local references/fragments | `scripts/audit-site.mjs` | **FIXED** |
| DEF-015 | MEDIUM | Imported records/quests can throw on malformed optional timestamps | Observatory import | `living-time-observatory-workspace.js` | Optional dates were converted without validity fallback | Import otherwise-valid JSON containing an invalid `updatedAt` or quest `createdAt` | Invalid optional dates normalize safely; invalid required record dates are rejected | syntax + workspace validation | **FIXED** |
| DEF-016 | BLOCKER | Dedicated Sphere reports `DUPLICATE_CANVAS` after rendering a valid first frame and falls back to SVG | `living-time-sphere.html` on Android/WebGL | capability manager, effects, renderer 3D, and UI lifecycle | Repeated diagnostics allocated temporary WebGL contexts; context-loss retries appended a new canvas without disposing the interrupted generation | Open the full Sphere on Android and inspect Technical Details after automatic recovery; before repair `canvasCount` could rise with every generation | Capability probing is cached, temporary probe contexts are released, retries dispose stale GPU state, and exactly one generation-tagged canvas remains | `tests/sphere-renderer-lifecycle-hotfix.test.js` plus renderer/reliability suites | **FIXED — SAME-DEVICE 3D CONFIRMED** |
| DEF-017 | BLOCKER | Loading a device location makes the Android Observatory tab unresponsive | `living-time-sphere.html` with an active place | live-data snapshot, Open-Meteo adapter, location command, and UI environment listeners | Snapshot reads initiated refreshes; loading was synchronously dispatched before the in-flight guard existed; the resulting environment event read another snapshot and recursively started another refresh | Open the full 3D Sphere on Android, tap `Use My Location`, grant permission, and wait for weather loading | One bounded provider request runs per place, progress stays visible, model reads stay pure, and the 3D Sphere and page remain responsive | `tests/environmental-sphere-workflow.test.js` re-entry, purity, and removal regressions | **FIXED — SAME-DEVICE LOCATION RECHECK REQUIRED** |
| DEF-018 | HIGH | Witnesses and recent actions recorded during UTC/local rollover can appear on the wrong day in the seven-day path | site-wide Codex Memory | `docs/assets/js/codex-memory.js` | Stored ISO timestamps were grouped by their UTC date prefix while the seven-day calendar is built from local dates | Record an action after UTC midnight but before local midnight, then inspect today's marker | Timestamped activity is converted to the visitor's local calendar date before grouping | `tests/living-codex-phase2.test.js` | **FIXED** |
| DEF-019 | HIGH | Top Today appears active but does not return the selected calendar day to Today | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js` | The control requested only a view/camera transition; an already-active Today mode became a no-op, and range presets would not reset after a year change | Move to another day and tap Today; before repair the selected day, URL marker, details, and 3D focus remain historical | Every Today/Now entry point commits one canonical live day/year/marker/URL transaction | `tests/living-time-sphere-temporal-navigation.test.js` | **FIXED — PHONE RECHECK REQUIRED** |
| DEF-020 | MEDIUM | Data Table/Text choices are empty and camera presets can contradict the active view button | `living-time-sphere.html` | Sphere UI alternate-view and camera handlers | Alternate surfaces were only shown/hidden without population; camera buttons called the renderer semantic-mode API | Select Data Table/Text or use Top/Tilted/Edge, then compare visible content and active mode | Alternate views contain canonical coordinates; camera presets move only the camera | `tests/living-time-sphere-temporal-navigation.test.js`, `tests/pr3-completion-pass.test.js` | **FIXED** |
| DEF-021 | BLOCKER | Full Sphere remains on `Aligning Living Time`; camera controls stay disabled and environment status never leaves syncing | `living-time-sphere.html` | `docs/assets/js/sphere/living-time-sphere-ui.js` | Temporal model decoration resolved `todayPosition` but returned an undefined `todayPatternPosition` shorthand, throwing before the first baseline render | Apply release `.4` and open the full Sphere; the workspace initializes but neither SVG nor 3D paints | Model decoration must return the resolved live coordinate and the full-page boot path must execute in regression coverage | Integrated boot execution inside `tests/living-time-sphere-temporal-navigation.test.js` | **FIXED IN 2026.08.15.5 — PHONE RECHECK REQUIRED** |
| DEF-022 | HIGH | Deep Time map forces horizontal scrolling, crowds decade labels at longer spans, and becomes excessively tall when empty | `living-time-sphere.html` on mobile | Observatory workspace chart geometry and CSS | Fixed 1,100-unit geometry, 720px minimum SVG width, invariant 10-year ticks, and a flow-positioned empty state | Select 200–500 years on a phone and scroll the map | Geometry and tick cadence follow container width/span; empty guidance overlays the plot without extending it | Responsive layout assertions in `tests/advanced-observatory-rebuild.test.js` | **FIXED IN 2026.08.15.5 — PHONE RECHECK REQUIRED** |
| DEF-023 | BLOCKER | WebGL1-only Android enters the 3D path although Three r167 can create only WebGL2, then fails during renderer construction | full Sphere and shared mounts | capability manager, effects, mount, renderer 3D | Capability selection tested generic WebGL while the vendored renderer requires WebGL2 | Emulate a browser where `webgl` succeeds and `webgl2` returns null | Route directly to the accessible SVG renderer with a specific WebGL2 reason | `tests/living-time-calendar-workbench.test.js`, capability/reliability suites | **FIXED IN 2026.08.15.6** |
| DEF-024 | BLOCKER | A timed-out async renderer can finish later, append a stale canvas, or leave the retry poll alive forever | full Sphere on slow imports/layout | renderer 3D and Sphere UI | `Promise.race` did not cancel initialization and retry polling had no elapsed/poll cap | Hold the renderer init promise unresolved beyond 25 seconds | Timeout invalidates the init epoch, tears down partial state, caps polling, and settles on SVG | calendar release lifecycle assertions plus existing renderer lifecycle suite | **FIXED IN 2026.08.15.6** |
| DEF-025 | CRITICAL | Temporal playback and repeated layer rebuilds accumulate geometries/materials and WebGL contexts | full Sphere 3D | renderer 3D and animation controller | Removed group children and teardown did not dispose scene resources or force context loss; still mode kept a perpetual RAF | Run playback/layer changes repeatedly and inspect GPU/RAF lifetime | Dispose dynamic object trees, release orphan contexts, force context loss on teardown, detach listeners, and quiesce clean still frames | calendar release lifecycle assertions and full renderer suite | **FIXED IN 2026.08.15.6** |
| DEF-026 | HIGH | Browser Back/Forward can show URL settings that disagree with the running timezone, boundary, layers, renderer, labels, motion, or camera | full Sphere deep links | Sphere UI URL-state application | `popstate` restored only year/view/marker and reused stale values for absent fields | Change multiple serialized controls, navigate history, then compare URL, controls, and model | One parsed-state transaction restores all serialized fields, defaults absent values, and never rewrites the popped entry | `tests/living-time-calendar-workbench.test.js` | **FIXED IN 2026.08.15.6** |
| DEF-027 | HIGH | Calendar exploration lacks one synchronized civil/Pattern workbench and portable private agenda | full Sphere | Calendar Atlas module and CSS | Navigation existed across separate controls without a unified Day/Week/Moon/Year crosswalk | Select a civil date, inspect outside days, compare years, or move between scales | One accessible local-first Atlas keeps all scales and the Sphere synchronized, preserves outside-day truth, and supports deliberate `.ics` portability | `tests/living-time-calendar-workbench.test.js` | **FIXED IN 2026.08.15.6** |
| DEF-028 | MEDIUM | The configured `18:00` “sunset” boundary is not automatically replaced by the astronomical sunset for the active coordinates | full Sphere calendar | boundary configuration and environment provider | Calendar boundary is synchronous/manual; provider sunset is a separate asynchronous observation | Compare configured boundary with forecast sunset on a date where they differ | Keep the manual boundary explicit and show provider sunset separately until a tested astronomical/manual/fallback policy is introduced | calendar boundary regressions; provider sunset mapping tests | **OPEN — TRUTHFULLY LABELED / NO SILENT SUBSTITUTION** |

## Runtime error capture summary

- `index.html`: baseline production inspection exposed the blank Sphere and broken deployed images; rebuilt source now mounts the progressive renderer and collapses failed media safely.
- `moons.html`: no app-breaking errors; blocked third-party font/GTM loads only.
- `living-time-sphere.html`:
  - Before repair: `ReferenceError: _selectedDayFromMarker is not defined`.
  - Initial repair: no app-breaking runtime exceptions observed in the original startup path.
  - Advanced rebuild: shared renderer lifecycle reports baseline, upgrade, active 3D, fallback, and context loss; local workspace shells are fully bound.
  - Mobile renderer hotfix: repeated WebGL probes and orphaned retry canvases are eliminated; same-device 3D recheck passed.
  - Mobile location hotfix: recursive environment refresh dispatch is eliminated in release `2026.08.15.3`; repeat the device-location flow before merge.
  - Temporal Lens phase: release `2026.08.15.4` makes Today authoritative across mode/range/history controls, fills alternate renderers, and adds exact circular navigation; repeat the listed phone checks before merge.
  - Sphere boot hotfix: release `2026.08.15.5` repairs the `.4` model-decoration `ReferenceError`, adds integrated full-page boot execution, and makes the 50–500-year chart responsive; repeat the Android checks before merge.
  - Calendar Atlas and renderer-hardening release: `2026.08.15.6` adds the synchronized local-first calendar, makes WebGL2 authoritative, cancels late init generations, caps recovery polling, releases dynamic GPU resources/listeners, idles clean still frames, and restores complete browser-history state.

## Notes

- Core blocker resolution prioritized first root runtime error to prevent cascaded secondary failures.
- Optional subsystems continue to degrade without preventing core calendar and renderer operation.
- A production deployment is required to visually confirm the new artifact under real CDN headers and each target GPU class.
