# Remnant Phase 1 Device Validation Checklist

All checks below require physical device/browser confirmation and are marked **REQUIRES OWNER DEVICE TEST** until performed on target hardware.

## Android Chrome (browser)
- [ ] REQUIRES OWNER DEVICE TEST — Scrolling from ordinary text and empty areas
- [ ] REQUIRES OWNER DEVICE TEST — Tab navigation and keyboard traversal
- [ ] REQUIRES OWNER DEVICE TEST — Date controls
- [ ] REQUIRES OWNER DEVICE TEST — Oracle form flow and result reveal
- [ ] REQUIRES OWNER DEVICE TEST — Technical disclosure and nested raw JSON disclosure
- [ ] REQUIRES OWNER DEVICE TEST — Native share and fallback behavior
- [ ] REQUIRES OWNER DEVICE TEST — Clipboard fallback
- [ ] REQUIRES OWNER DEVICE TEST — Share image export (square / portrait / story)
- [ ] REQUIRES OWNER DEVICE TEST — Seal SVG/PNG export controls
- [ ] REQUIRES OWNER DEVICE TEST — Service worker update and offline reload
- [ ] REQUIRES OWNER DEVICE TEST — Saved profile persistence and witness-log persistence

## Android installed PWA
- [ ] REQUIRES OWNER DEVICE TEST — Launch and route persistence
- [ ] REQUIRES OWNER DEVICE TEST — Offline reload
- [ ] REQUIRES OWNER DEVICE TEST — Share fallback behavior
- [ ] REQUIRES OWNER DEVICE TEST — Service-worker update prompt and refresh

## iPhone Safari
- [ ] REQUIRES OWNER DEVICE TEST — Scrolling in all sections and empty space
- [ ] REQUIRES OWNER DEVICE TEST — Date controls and Oracle form inputs
- [ ] REQUIRES OWNER DEVICE TEST — Technical disclosure interaction
- [ ] REQUIRES OWNER DEVICE TEST — Clipboard/native share fallback behavior
- [ ] REQUIRES OWNER DEVICE TEST — Share image and seal export controls

## iPhone Home Screen mode
- [ ] REQUIRES OWNER DEVICE TEST — Installed app launch and navigation
- [ ] REQUIRES OWNER DEVICE TEST — Offline reload and cache update behavior
- [ ] REQUIRES OWNER DEVICE TEST — Saved-profile and witness-log persistence

## Desktop Chrome
- [ ] REQUIRES OWNER DEVICE TEST — Keyboard focus order and contrast
- [ ] REQUIRES OWNER DEVICE TEST — Long content overflow and responsive behavior
- [ ] REQUIRES OWNER DEVICE TEST — Print mode
- [ ] REQUIRES OWNER DEVICE TEST — Reduced motion behavior

## Desktop WebKit/Safari equivalent
- [ ] REQUIRES OWNER DEVICE TEST — Form and disclosure behavior
- [ ] REQUIRES OWNER DEVICE TEST — Share and export controls
- [ ] REQUIRES OWNER DEVICE TEST — Layout stability with long content
